<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itesm.mx.racm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegistrarPerfil">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CrearContacto" />
        <activity android:name=".VerFoto" />
        <activity android:name=".EditarPerfil" />
        <activity android:name=".PerfilDetalle" />
        <activity android:name=".CambiarFoto"/>
        <activity android:name=".EditarContacto"/>


    </application>

</manifest>